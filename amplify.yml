version: 1
frontend:
  phases:
    build:
      commands:
        - export PATH="$PATH:`pwd`/flutter/bin"
        - flutter pub get
        - flutter build web
  artifacts:
    baseDirectory: build/web
    files:
      - '**/*'
